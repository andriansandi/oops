{
  "name": "oops",
  "version": "0.1.0",
  "description": "Multi-Instance DB Management Hub — TUI for Cloudflare D1 & friends",
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "bun run packages/cli/src/index.ts",
    "start": "bun run packages/cli/src/index.ts",
    "typecheck": "bun --bun tsc -p packages/cli/tsconfig.json && bun --bun tsc -p packages/core/tsconfig.json",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "1.3.14",
    "typescript": "^5.4.0"
  }
}
